using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Timeline;


[TrackClipType(typeof(TestLightControlAsset))]  //使用的的轨道资产
[TrackBindingType(typeof(Light))]  //轨道上绑定的组件类型

public class LightControlAtrack : TrackAsset
{
    // Start is called before the first frame update
    
   
}
